The following changes were made in this version:

| Type | Description |
| ---- | ----------- |
| feature | A project's name, URL, tags and archived state can now be edited together in a new "Edit project" dialog, which replaces the separate rename action on the "Print Projects" page. Tags and the archived state also stay available as quick actions. |
| feature | The dialogs for creating a project from files and from a ZIP archive now have an optional field for the URL of the project page. |
| feature | STEP files (`*.stp` and `*.step`) are now recognized as 3D model files, so they are listed under "3D Model Files" and can be selected when creating a slicer project. |

[comment]: # (Use one of the following types: feature, bugfix, tech)
